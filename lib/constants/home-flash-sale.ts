/**
 * Nama kampanye flash sale untuk blok hero beranda (`HomeFlashSaleBlock`).
 * Harus sama dengan `flash_sales.name` di Supabase (lihat juga alias `!` di `fetchFlashSaleBlockByCampaignName`).
 */
export const HOME_HERO_FLASH_SALE_CAMPAIGN_NAME = "Flash Sale Mei 2026";
