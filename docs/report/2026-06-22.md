# Report — 2026-06-22

## Ringkasan

Migrasi hosting **production** GeekyTech dari Vercel ke VPS GoDaddy (AlmaLinux 10 + cPanel/WHM, IP `184.168.121.243`) selesai end-to-end. `geeky.id` sekarang live di VPS dengan SSL, auto-deploy dari `main`, dan webhook Midtrans/Biteship sudah diverifikasi jalan.

Branch `development` tetap di Vercel (preview). Hanya `main` yang ke VPS.

## Yang dikerjakan hari ini

### Akses & keamanan VPS
- SSH key-based access untuk `root` dan `geeky` (password auth dimatikan total, `PermitRootLogin prohibit-password`)
- Firewall `firewalld` (CSF gagal diinstall — domain resmi `download.configserver.com` NXDOMAIN di semua DNS publik, kemungkinan masalah di sisi ConfigServer, bukan server kita)
- `fail2ban` aktif untuk SSH (sempat mendeteksi percobaan brute-force nyata sebelum dipasang)
- MariaDB (3306) yang tadinya exposed ke `0.0.0.0` sekarang diblokir dari luar
- Private key admin & semua detail akses tersimpan di `docs/vps-access/README.md` (gitignored)

### Setup hosting app
- Akun cPanel `geeky` dibuat untuk domain `geeky.id`
- **cPanel "Setup Node.js App" tidak bisa dipakai** — modul `Cpanel::API::NodejsSelector` belum ada di build cPanel untuk AlmaLinux 10 (sudah coba update cPanel ke versi terbaru, tetap tidak ada — limitasi platform, bukan bug kita)
- Solusi: Next.js app dijalankan via **systemd service** (`geekytech-web.service`, user `geeky`, Node 22 dari `ea-nodejs22`), **Apache (dikelola cPanel) reverse-proxy** ke `127.0.0.1:3000` lewat userdata include (harus diisi di DUA tempat: `std` untuk port 80 dan `ssl` untuk port 443 — sempat lupa isi `ssl` di awal sehingga HTTPS masih nunjukin default Apache listing)
- Repo di-clone via SSH deploy key read-only (`github_deploy`), branch `main`

### Bug ditemukan & diperbaiki
- Build pertama gagal — type error di `notifications-panel.tsx`. Ternyata `main` ketinggalan **49 commit** dari `development` (termasuk fix bug ini). Di-merge `development → main`, push, build ulang sukses.
- `.env.production` divalidasi terhadap pemakaian aktual di kode (bukan cuma `env.example` yang ketinggalan): ditambahkan `CRON_SECRET` (baru, untuk `/api/cron/expire-orders`), `BITESHIP_WEBHOOK_SECRET`, disinkronkan `BITESHIP_ORIGIN_POSTAL` vs `BITESHIP_ORIGIN_POSTAL_CODE` (kode pakai dua nama beda di file berbeda — `app/api/shipping/rates/route.ts` cuma cek satu nama tanpa fallback, jadi sempat diam-diam pakai default hardcode `10110`). `NEXTAUTH_SECRET` yang masih placeholder `your_...` diganti value random asli.

### DNS cutover & SSL
- A record `geeky.id` + `www` di GoDaddy diarahkan dari IP Vercel (`216.198.79.1`) ke VPS (`184.168.121.243`) — propagasi <5 menit
- AutoSSL (Let's Encrypt) aktif, force-redirect HTTP→HTTPS (301) ditambahkan manual

### Auto-deploy
- GitHub Actions (`.github/workflows/deploy-production.yml`), trigger `push` ke `main`
- SSH key khusus deploy (`github-actions-deploy`) dibatasi **forced-command** (`command="/home/geeky/deploy.sh"` di `authorized_keys`) — sudah ditest, command sembarang yang dikirim tetap diabaikan, server cuma jalankan `deploy.sh`
- `geeky` dapat sudo NOPASSWD **hanya** untuk `systemctl restart geekytech-web` (`/etc/sudoers.d/geeky-deploy`), tidak ada akses sudo lain
- Sudah ditest end-to-end: push ke `main` → auto pull+build+restart, sukses

### Smoke test webhook
Dijalankan langsung dari VPS (secret tidak pernah keluar ke chat):
- Midtrans: signature salah → 401, signature valid (order tidak ada) → 200 `ok:true`
- Biteship: token salah → 401, token valid → 200 `ok:true`, lewat root-rewrite (`proxy.ts` intercept `POST /`) → 200, installation ping → 200

## Belum dikerjakan (lanjutan)

Lihat [task-maintenance.md](../task-maintenance.md).
